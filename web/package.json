{
  "name": "eyeball-web",
  "version": "0.2.0",
  "private": true,
  "description": "eyeball: paste a URL, get scored on how AI-generated your design looks. Free deterministic scan + a paid Claude vision read, behind a durable spend cap.",
  "type": "module",
  "scripts": {
    "test": "node test/api-smoke.mjs"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.105.0",
    "@sparticuz/chromium": "^131.0.0",
    "@upstash/redis": "^1.38.0",
    "puppeteer-core": "^23.0.0"
  }
}
